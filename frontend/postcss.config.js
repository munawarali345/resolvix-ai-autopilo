/**
 * PostCSS Configuration
 * Processes CSS with Tailwind CSS and Autoprefixer
 */
export default {
  plugins: {
    // Tailwind CSS plugin - processes Tailwind directives and classes
    tailwindcss: {},
    // Autoprefixer - adds vendor prefixes for browser compatibility
    autoprefixer: {},
  },
};
